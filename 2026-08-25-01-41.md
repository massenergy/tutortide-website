# Implementation Plan — Replace `massenergy2050@gmail.com` with `info@tutortide.online`

**Date:** 2026-08-25 01:41
**Scope:** Every occurrence of `massenergy2050@gmail.com` in the tutortide repo.
**Branch:** `feature/next-4` (current), landing to `main`.

---

## Assumptions (please confirm)

1. **`info@tutortide.online` is a live, monitored mailbox.** This change puts the address on the public
   site, in `mailto:` links, in the meta description, and in the Organization schema.org JSON-LD that
   Google reads. If the mailbox is not receiving mail yet, every enquiry sent after deploy is lost
   silently. **This is the one thing to verify before we ship, not after.**
2. **Git author identity is out of scope by default.** `git config --global user.email` is also
   `massenergy2050@gmail.com`. That is your commit-authorship identity, not a website contact detail —
   it is *not* in any repo file, and changing it would alter how future commits attribute to your
   GitHub account. See "Optional / decision needed" below.
3. **Historical planning notes get updated too.** You said "everywhere", so the plan includes the three
   hits in the old planning doc. Flagged separately because they are a record of a past decision, not
   live config.
4. **`_archive/index.legacy.html` is out of scope.** It contains no gmail address. It does contain a
   pre-existing inconsistency (`.com` vs `.online`) noted at the end.

---

## Full inventory — every occurrence found

Search performed: `grep -rn "massenergy2050@gmail\.com"` across the whole working tree excluding
`.git/`. **5 occurrences across 3 files.**

### Group A — Live site code (2 occurrences, 2 files) — the ones that actually matter

| # | File | Line | Current content | What it drives |
|---|------|------|-----------------|----------------|
| A1 | `src/data/site.ts` | 7 | `email: "massenergy2050@gmail.com",` | **The single source of truth.** Fans out to 5 render sites (see below). |
| A2 | `src/pages/contact.astro` | 9 | `description="… Email massenergy2050@gmail.com."` | **Hardcoded duplicate.** The `<meta name="description">` on `/contact`, which is what Google shows in search results. Does *not* read from `site.ts`. |

**Why A1 is only one line but changes five places on the site.** `site.email` is imported and rendered by:

| Consumer | Line | Rendered as |
|----------|------|-------------|
| `src/components/Footer.astro` | 35 | `href={mailto:${site.email}}` — footer link target |
| `src/components/Footer.astro` | 38 | `{site.email}` — footer visible text |
| `src/pages/contact.astro` | 29 | `href={mailto:${site.email}}` — contact link target |
| `src/pages/contact.astro` | 30 | `{site.email}` — contact visible text |
| `src/pages/contact.astro` | 46 | `mailto:${site.email}?subject=Project%20enquiry` — "Email us" button |
| `src/layouts/BaseLayout.astro` | 36 | `email: site.email` in Organization JSON-LD — **on every page of the site** |

The footer is in the shared layout, so the address currently appears on **all 11 pages**
(`/`, `/about`, `/products`, `/contact`, `/jinjing`, `/nyokavi`, `/waterpro`, `/tundamoto`,
`/styleguide`, `/404`, plus the JSON-LD). Editing `site.ts:7` fixes all of them at once —
**no per-page edits needed.**

### Group B — Historical planning document (3 occurrences, 1 file)

| # | File | Line | Context |
|---|------|------|---------|
| B1 | `2026-07-12_14-43-19.md` | 51 | `**A4.** Contact + "sign up" are **mail-to links to \`massenergy2050@gmail.com\`** for now` |
| B2 | `2026-07-12_14-43-19.md` | 160 | `/contact — contact details + **mail-to link to \`massenergy2050@gmail.com\`**` |
| B3 | `2026-07-12_14-43-19.md` | 292 | `4. **Contact / sign-up:** mail-to \`massenergy2050@gmail.com\`, no form backend.` |

These are a July 2026 planning record describing what the site looked like at that time. Updating them
is harmless and satisfies "everywhere"; it does mildly rewrite history. **If you'd rather leave the
record intact, say so and I'll skip Phase 2** — nothing else depends on it.

### Not an occurrence, but adjacent (no action unless you say so)

| Where | Content | Note |
|-------|---------|------|
| `git config --global user.email` | `massenergy2050@gmail.com` | Commit authorship, not a repo file. Changing it affects GitHub commit attribution for all your projects, not just this one. |
| `2026-07-12_14-43-19.md:20,48,184` | `massenergy.github.io`, `github.com/massenergy/...` | GitHub *username*, not an email. Leave alone. |
| `_archive/index.legacy.html:500` | `href="mailto:info@tutortide.com"` | Pre-existing bug: link target says `.com`, visible text on line 503 says `.online`. Archived file, not built or served. |
| `src/pages/_jinjing-clone.html:1` | `mailto:info@tutortide.online` | Already correct. No change. |

---

## Phase 1 — Live site code

**Changes:**

1. `src/data/site.ts:7` — `email: "massenergy2050@gmail.com",` → `email: "info@tutortide.online",`
2. `src/pages/contact.astro:9` — `Email massenergy2050@gmail.com.` → `Email info@tutortide.online.`

**Verification before moving on:**
- `grep -rn "massenergy2050@gmail.com" src/` returns nothing.
- `npm run build` completes with no errors.
- `grep -rc "info@tutortide.online" dist/**/*.html` — expect the address present on every built page
  (footer + JSON-LD), and `grep -rn "massenergy2050" dist/` returns nothing.

## Phase 2 — Historical planning document

**Changes:** `2026-07-12_14-43-19.md` lines 51, 160, 292 — replace the address in-place, leaving all
surrounding wording untouched.

**Verification:** `grep -rn "massenergy2050@gmail.com" . --exclude-dir=.git` returns **zero results
across the entire repo.**

## Phase 3 — Build and visual check

**Steps:**
1. `npm run build`
2. `npm run preview`, then check in a browser:
   - `/contact` — the three touchpoints (address text, address link, "Email us" button)
   - `/` — footer address text and link
   - View source on any page — `"email":"info@tutortide.online"` inside the JSON-LD block
   - View source on `/contact` — `<meta name="description">` contains the new address

**Verification:** every `mailto:` on the site opens a compose window addressed to
`info@tutortide.online`; no gmail address appears anywhere in page source.

## Phase 4 — Land the work

No CI, no deploy script — deployment is manual Firebase Hosting (`firebase.json` → `public: "dist"`,
project `tutortide-5a652`). Following your standing merge-deploy-branch cadence.

**⚠ Note on `git add`:** the repo root currently has 6 untracked files (five funding-related PDFs and
`Two things need you.odt`). A blanket `git add -A` would commit all of them into this email-change
commit. **I will stage the changed files by name instead.**

**Steps:**
```bash
git add src/data/site.ts src/pages/contact.astro 2026-07-12_14-43-19.md 2026-08-25-01-41.md
git commit -m "Replace gmail contact address with info@tutortide.online"
git checkout main && git merge feature/next-4
npm run build
firebase deploy --only hosting --project tutortide-5a652
git push
```

**Verification (assert the end state, don't infer from exit code):**
- `git rev-parse HEAD` equals `git rev-parse origin/main`
- `git status --short` shows only the 6 pre-existing untracked files — no staged or modified entries
- Fetch the deployed page and confirm the new address is actually live:
  `curl -s https://tutortide.online/contact | grep -c "info@tutortide.online"` → non-zero
  `curl -s https://tutortide.online/contact | grep -c "massenergy2050"` → **zero**
- Repeat the same two greps against `https://tutortide.online/` for the footer

**Expected failures:** none. There is no test suite and no CI, so a red result at any step is a real
problem, not a known gate.

**Then:** `git checkout -b feature/next-5` off updated `main`, per your standing cadence.

---

## Verification summary

**Automated:**
```bash
grep -rn "massenergy2050@gmail.com" . --exclude-dir=.git    # expect: no output
npm run build                                                # expect: clean build
grep -rn "massenergy2050" dist/                              # expect: no output
grep -rn "info@tutortide.online" dist/ | wc -l               # expect: >= 11 (footer on every page)
```
No test suite exists in this project — `package.json` defines only `dev`, `build`, `preview`, `astro`.
The build succeeding plus the greps above **is** the verification.

**Manual:**
1. `npm run preview` → open `/contact`, click the address link and the "Email us" button; confirm both
   compose to `info@tutortide.online` (the button should also prefill subject "Project enquiry").
2. Open `/` and any product page (e.g. `/nyokavi`); confirm the footer address reads and links correctly.
3. View source on any page; find the `application/ld+json` block; confirm `"email"` field.
4. After deploy: visit `https://tutortide.online/contact` and repeat steps 1–3 against production.
5. **Send a real test email to `info@tutortide.online` and confirm it arrives.** This is the step that
   catches assumption #1 being wrong.

---

## Progress checklist

- [ ] Confirm `info@tutortide.online` is a live, monitored mailbox (assumption #1)
- [ ] Decide on Phase 2 — update the historical planning doc, or leave the record intact?
- [ ] Decide on git author identity — change `user.email`, or leave it?
- [ ] Phase 1 — `src/data/site.ts:7`
- [ ] Phase 1 — `src/pages/contact.astro:9`
- [ ] Phase 1 verification — grep `src/` clean, build passes
- [ ] Phase 2 — `2026-07-12_14-43-19.md` lines 51, 160, 292
- [ ] Phase 2 verification — repo-wide grep returns zero
- [ ] Phase 3 — build, preview, check `/contact` + footer + JSON-LD + meta description
- [ ] Phase 4 — stage by name (not `-A`), commit, merge to `main`
- [ ] Phase 4 — build + `firebase deploy --only hosting`
- [ ] Phase 4 — verify live: curl greps pass on `/contact` and `/`
- [ ] Phase 4 — `main` clean, `HEAD == origin/main`, push complete
- [ ] Phase 4 — send test email to the new address and confirm receipt
- [ ] Branch `feature/next-5` off updated `main`
